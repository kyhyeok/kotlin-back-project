jwt:
  secret-key: "test"
  time : 30

oauth2:
  providers:
    google:
      client-id: "<TODO>"
      client-secret: "<TODO>"
      redirect-uri: http://localhost:9090/api/v1/auth/callback
    github:
      client-id: "<TODO>"
      client-secret: "<TODO>"
      redirect-uri: http://localhost:9090/api/v1/auth/callback

database:
  redis:
    host: localhost
    port: 6379
    password : ""
    database : 0
    timeout : 10000
  redisson:
    host: redis://127.0.0.1:6379
    timeout: 10000
    password : ""
  mysql:
    url: jdbc:mysql://localhost:3306/bank?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  kafka:
    bootstrap-servers: localhost:9092

server.port: 9090